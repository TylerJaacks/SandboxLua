using Sandbox;

namespace SandboxLua.Core.SandboxLua.Utils
{
    class PlayerUtils
    {
        // TODO: Need to actually get the Player.
        public static Player GetPlayerFromName(string playerName)
        {
            return new Player();
        }
    }
}
